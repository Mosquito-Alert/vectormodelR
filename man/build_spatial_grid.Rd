% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_spatial_grid.R
\name{build_spatial_grid}
\alias{build_spatial_grid}
\title{Build a regular grid for a supplied polygon}
\usage{
build_spatial_grid(
  map = NULL,
  cellsize_m = 400,
  clip = TRUE,
  return_crs = 4326,
  shape = c("hex", "square"),
  write = TRUE,
  iso3 = NULL,
  admin_level = NULL,
  admin_name = NULL,
  data_dir = "data/proc"
)
}
\arguments{
\item{map}{Optional \code{sf} object describing the polygon(s) to cover.}

\item{cellsize_m}{Numeric. Grid cell size in meters (default 400).}

\item{clip}{Logical. If TRUE (default), intersect hexes with the polygon’s
outline.}

\item{return_crs}{Coordinate reference system for the output (default 4326).}

\item{shape}{Character. "hex" (default) for hexagons or "square" for squares.}

\item{write}{Logical. If TRUE, saves the generated grid to \code{data_dir}
using the slug-based filename (for example
\verb{spatial_<slug>_hex_grid_400.Rds}).}

\item{iso3}{Optional three-letter ISO3 code used with \code{admin_level} and
\code{admin_name} to locate boundary files when \code{map} is not supplied.}

\item{admin_level}{Optional administrative level corresponding to the
boundary file when \code{map} is not supplied.}

\item{admin_name}{Optional administrative unit name corresponding to the
boundary file when \code{map} is not supplied.}

\item{data_dir}{Directory where boundary inputs and grid outputs are stored.
Defaults to \code{"data/proc"}.}
}
\value{
An \code{sf} object with polygons and a \code{grid_id} column. A
size-specific \verb{grid_id_<cellsize>} column is also added to support storing
multiple grid resolutions side-by-side.
}
\description{
Creates a tessellation (hexagonal or square) at a given cell size (meters),
optionally clips the grid to the polygon footprint, and returns the grid in the desired
CRS. Uses \code{convert_to_utm()} to pick an appropriate metric projection.
}
\examples{
\dontrun{
library(sf)

# Example polygon (Barcelona bbox)
bcn_poly <- st_as_sfc(st_bbox(c(
  xmin = 2.05, xmax = 2.25,
  ymin = 41.30, ymax = 41.50
)), crs = 4326)

# Hexagonal grid at 400 m cells, returning sf object
hex_grid <- build_spatial_grid(bcn_poly, cellsize_m = 400)

# Square grid at 1 km cells, saving to disk
sq_grid <- build_spatial_grid(
  map = bcn_poly,
  cellsize_m = 1000,
  shape = "square",
  write = FALSE
)

# Using stored administrative boundaries (requires matching RDS file)
hex_grid <- build_spatial_grid(
  iso3 = "ESP",
  admin_level = 2,
  admin_name = "Barcelona",
  write = TRUE
)
}
}
