% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize_vector_dataset.R
\name{initialize_vector_dataset}
\alias{initialize_vector_dataset}
\title{Build a unified vector presence dataset}
\usage{
initialize_vector_dataset(
  iso3,
  admin_level,
  admin_name,
  sources = c("malert", "gbif"),
  vector_dirs = "data/proc",
  output_dir = "data/proc",
  overwrite = FALSE,
  exclude_collection_on_merge = "Mosquito Alert Tiger Mosquito"
)
}
\arguments{
\item{iso3}{Three-letter ISO3 country code used in the vector filenames.}

\item{admin_level}{Administrative level included in the vector filenames.}

\item{admin_name}{Administrative unit name used in the vector filenames.}

\item{sources}{Character vector specifying which vector sources to include.
Accepted values are \code{"malert"}, \code{"gbif"}, or both. Default includes both.}

\item{vector_dirs}{Character vector of directories to search for the prepared
vector files. Defaults to \code{"data/proc"}.}

\item{output_dir}{Directory where the combined dataset will be saved.
Defaults to \code{"data/proc"}.}

\item{overwrite}{Logical; set to \code{TRUE} to overwrite an existing output file.
Defaults to \code{FALSE}.}

\item{exclude_collection_on_merge}{Character vector of GBIF \code{collectionCode}
values to drop when both sources are merged. Defaults to discarding
\code{"Mosquito Alert Tiger Mosquito"} when Mosquito Alert data is present. Use
\code{NULL} to keep all collection codes.}
}
\value{
A tibble with columns \code{date}, \code{year}, \code{lon}, \code{lat},
\code{presence}, \code{referenceID}, and \code{source}. Attributes include \code{output_path}
and \code{source_files}.
}
\description{
Combines previously prepared Mosquito Alert and/or GBIF vector observations
for a given location into a single presence-only dataset with harmonised
columns. The helper looks for the size-specific vector files generated by
\code{get_malert_data()} and \code{get_gbif_data()} and merges them when requested.
}
