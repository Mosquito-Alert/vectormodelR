% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_adm_perimeter.R
\name{get_adm_perimeter}
\alias{get_adm_perimeter}
\title{Compute ADM polygon perimeters using local UTM projections}
\usage{
get_adm_perimeter(
  sf_obj,
  output_filename = NULL,
  rds = TRUE,
  proc_dir = "data/proc",
  verbose = TRUE
)
}
\arguments{
\item{sf_obj}{An \code{sf} object containing polygon geometries with a defined CRS.}

\item{output_filename}{Optional file name to use when persisting the result
(when \code{rds = TRUE}). If \code{NULL}, defaults to \code{"perim_adm.rds"}.}

\item{rds}{Logical. If \code{TRUE}, save the computed layer as an \code{.rds} file under
\code{proc_dir}. Defaults to \code{TRUE}.}

\item{proc_dir}{Directory where the \code{.rds} file should be written when
\code{rds = TRUE}. Default is \code{"data/proc"}.}

\item{verbose}{Logical. Print messages when saving the \code{.rds} file. Defaults to TRUE.}
}
\value{
An \code{sf} object with columns: \code{perimeter_m}, \code{area_m2}, and the
original \code{geometry} transformed back to EPSG:4326.
}
\description{
For each feature in an \code{sf} object, this helper estimates the most
appropriate UTM zone from the feature's centroid, reprojects the geometry,
and calculates both perimeter length and polygon area in metres. Optionally, the resulting
layer can be written to disk as an \code{.rds} file under \code{data/proc}.
}
\examples{
\dontrun{
gadm_sf <- get_gadm_data("ESP", 2, name = "Barcelona", rds = FALSE)
perim_sf <- get_adm_perimeter(gadm_sf, output_filename = "perim_barcelona.rds")
}

}
