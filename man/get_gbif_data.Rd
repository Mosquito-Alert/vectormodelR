% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_gbif_data.R
\name{get_gbif_data}
\alias{get_gbif_data}
\title{Download GBIF occurrence data for a taxon and country}
\usage{
get_gbif_data(
  taxon_key,
  iso3,
  admin_level,
  admin_name,
  year_min = NULL,
  year_max = NULL,
  desired_cols = NULL,
  out_dir = "data/proc",
  perimeter_dir = "data/proc",
  gbif_user = Sys.getenv("GBIF_USER", unset = NA_character_),
  gbif_pwd = Sys.getenv("GBIF_PWD", unset = NA_character_),
  gbif_email = Sys.getenv("GBIF_EMAIL", unset = NA_character_),
  clip_to_perimeter = TRUE,
  save_outputs = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{taxon_key}{Numeric vector of GBIF taxon keys (e.g. \code{c(1651430, 1651891)} for \emph{Aedes albopictus} and \emph{Aedes aegypti}).}

\item{iso3}{Three-letter ISO country code (e.g. \code{"ESP"}). It is converted to
the GBIF-required ISO2 code using \code{countrycode}.}

\item{admin_level}{Administrative level used when the perimeter was
generated. If \code{clip_to_perimeter = FALSE}, this value is optional.}

\item{admin_name}{Administrative unit name used for the perimeter. If
\code{clip_to_perimeter = FALSE}, this value is optional.}

\item{year_min}{Optional minimum occurrence year.}

\item{year_max}{Optional maximum occurrence year.}

\item{desired_cols}{Optional character vector (or list) of column names to
retain after perimeter filtering. Matching is case-insensitive and accepts
common typos for \code{occurrenceStatus}/\code{occurrenceID}. When \code{NULL} (default),
all columns are preserved.}

\item{out_dir}{Directory where both the raw GBIF download and the
perimeter-clipped table should be saved. Defaults to \code{"data/vector"}. The
unfiltered results are written as \verb{vector_<iso3>_gbif.Rds}; the
perimeter-clipped output becomes
\verb{vector_<iso3>_<admin_level>_<admin_name>_gbif.Rds} (with the
administrative name normalised to lowercase underscores).}

\item{perimeter_dir}{Directory containing the perimeter RDS artefacts. The
function looks for \verb{spatial_<slug>_perimeter.rds} in this location. Ignored
when \code{clip_to_perimeter = FALSE}.}

\item{gbif_user}{GBIF username. Defaults to \code{Sys.getenv("GBIF_USER")}.}

\item{gbif_pwd}{GBIF password. Defaults to \code{Sys.getenv("GBIF_PWD")}.}

\item{gbif_email}{GBIF-registered email. Defaults to
\code{Sys.getenv("GBIF_EMAIL")}.}

\item{clip_to_perimeter}{Logical; when \code{TRUE} (default) the download is
intersected with the administrative perimeter. Set to \code{FALSE} to retain
the countrywide results without requiring perimeter artefacts.}

\item{save_outputs}{Logical; when \code{TRUE} (default) the raw and clipped
datasets are written to disk. Use \code{FALSE} to return the data in memory only.}

\item{verbose}{Logical; if \code{TRUE} (default) progress messages are printed.}
}
\value{
Tibble of occurrence records. Metadata attributes include the GBIF
download key (\code{gbif_key}), the perimeter-clipped table path
(\code{gbif_table_path}), the raw unfiltered table path (\code{gbif_raw_table_path}),
the location slug (\code{location_slug}), and the perimeter source path
(\code{perimeter_source}).
}
\description{
Submits an authenticated GBIF download request for a given species and
country, waits for completion, retrieves the zipped archive, removes the
downloaded file after import, clips occurrences to the supplied perimeter,
and returns the parsed occurrence records.
GBIF credentials can be provided via arguments or
pulled from the \code{GBIF_USER}, \code{GBIF_PWD}, and \code{GBIF_EMAIL} environment
variables.
}
