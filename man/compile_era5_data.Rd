% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compile_era5_data.R
\name{compile_era5_data}
\alias{compile_era5_data}
\title{Compile ERA5 monthly CSVs from multi-variable GRIB/ZIP files}
\usage{
compile_era5_data(
  input_dir = NULL,
  processed_dir = NULL,
  iso3 = NULL,
  admin_level = NULL,
  admin_name = NULL,
  dataset = "reanalysis-era5-single-levels",
  recent_n = 3,
  verbose = TRUE
)
}
\arguments{
\item{input_dir}{Directory containing multi-variable GRIB files. If NULL/empty and \code{iso3}
supplied, defaults to \code{file.path("data/weather/grib", tolower(iso3))} or
\verb{file.path("data/weather/grib", <slug>)} when \code{admin_name} is provided.}

\item{processed_dir}{Output dir for monthly CSVs. Defaults to \verb{<input_dir>/processed}.
Within this, dataset-specific subdirectories are created: \verb{land/} and \verb{single-levels/}.}

\item{iso3}{Optional ISO3 code (character). Used to resolve \code{input_dir} (when missing)
and to build output filenames.}

\item{admin_level}{integer. GADM administrative level (0=country, 1=region, 2=province, ...).
Used only when \code{admin_name} is supplied.}

\item{admin_name}{character. Exact \verb{NAME_<level>} value to select within GADM.
When provided, uses admin-specific subdirectory and file naming.}

\item{dataset}{character. ERA5 dataset name ("reanalysis-era5-single-levels" or
"reanalysis-era5-land"). Required parameter - specifies which dataset to process.}

\item{recent_n}{Number of most recent monthly CSVs to merge into a summary (default 3).}

\item{verbose}{Print progress.}
}
\value{
(invisibly) a list with summary info.
}
\description{
Scans \code{input_dir} for ERA5 files (GRIB or ZIP format, with all variables in one file per month)
and converts each month into a single long-format CSV.GZ. Output is organized by dataset type:
\itemize{
\item ERA5 Single Levels → \verb{processed/single-levels/YYYY/}
\item ERA5-Land → \verb{processed/land/YYYY/}
}
}
\details{
This version handles the new CDS API format where all variables are bundled into a single
file per month (e.g., era5_ita_3_firenze_2022_12.grib containing all variables).

Also writes recent N-month summaries in \code{input_dir}.
}
