% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_brms_model.R
\name{run_brms_model}
\alias{run_brms_model}
\title{Fit a baseline Mosquito Alert occupancy model with brms}
\usage{
run_brms_model(
  dataset,
  priors = NULL,
  nchains = 4,
  threads_per_chain = 1,
  adapt_delta = 0.99,
  max_treedepth = 12,
  backend = c("cmdstanr", "rstan"),
  write_output = TRUE,
  output_path = NULL,
  verbose = TRUE
)
}
\arguments{
\item{dataset}{Either an in-memory data frame or the path to the RDS file
containing the model-preparation dataset.}

\item{priors}{Optional prior specification created with \code{brms::set_prior()}.
Defaults to \code{NULL}, which uses the brms defaults.}

\item{nchains}{Number of MCMC chains to run. Defaults to 4.}

\item{threads_per_chain}{Number of within-chain threads (requires the
\code{cmdstanr} backend). Defaults to 1 (single-threaded chains).}

\item{adapt_delta}{Target acceptance rate for the NUTS sampler. Defaults to
0.99.}

\item{max_treedepth}{Maximum tree depth for the NUTS sampler. Defaults to 12.}

\item{backend}{Sampling backend passed to \code{brms::brm()}. Defaults to
\code{"cmdstanr"} but \code{"rstan"} is supported as well.}

\item{write_output}{Logical; when \code{TRUE} the fitted model is saved to disk
using the same directory and stem as \code{dataset_path} with a
\verb{_brms_model.Rds} suffix. Defaults to \code{TRUE}.}

\item{output_path}{Optional path where the fitted model should be written. If
\code{NULL} (default) and \code{write_output} is \code{TRUE}, the path is auto-derived from
\code{dataset_path}.}

\item{verbose}{Logical; if \code{TRUE}, progress messages are emitted. Defaults to
\code{TRUE}.}
}
\value{
The fitted \code{brmsfit} object. The dataset used for modelling is stored
in the \code{model_data} attribute and the saved path (if any) is stored in the
\code{output_path} attribute.
}
\description{
Reads the enriched Mosquito Alert modelling dataset (either from disk or an
in-memory data frame), prepares the predictors used in the baseline
specification, and fits a Bernoulli-response model using \code{brms}. The function
assumes the input was generated by the package's feature engineering
pipeline and therefore contains the required columns (e.g. \code{presence},
\code{sea_days}, \code{maxTM}). The fitted model is returned invisibly and, by default,
also written to disk alongside the source dataset when a path is available.
}
