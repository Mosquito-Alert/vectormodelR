% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_brms_model.R
\name{run_brms_model}
\alias{run_brms_model}
\title{Fit a Standard Mosquito Alert occupancy model with brms}
\usage{
run_brms_model(
  dataset = NULL,
  formula = NULL,
  priors = NULL,
  nchains = 4,
  threads_per_chain = 1,
  adapt_delta = 0.99,
  max_treedepth = 15,
  backend = c("cmdstanr", "rstan"),
  iso3 = NULL,
  admin_level = NULL,
  admin_name = NULL,
  write_output = TRUE,
  output_path = "data/proc",
  input_dir = "data/proc",
  save_pars = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{dataset}{An in-memory modelling dataset (data.frame), a \code{bym2_data_prep} object,
or a path to the enriched RDS file.}

\item{formula}{Optional character string or formula object specifying the fixed
and random effects structure. If \code{NULL} (default), the function processes
\code{source} validation and uses a set of default predictors including
\code{sea_days}, \code{maxTM_z}, \code{ppt_z}, \code{ndvi_z}, \code{elev_z}, \code{pop_z}, \code{year}, and
\code{landcover_code}.}

\item{priors}{Optional \code{brms::set_prior} object. If \code{NULL}, default priors for
intercepts, coefficients, and standard deviations are used.}

\item{nchains}{Integer. Number of MCMC chains. Default 4.}

\item{threads_per_chain}{Integer. Number of threads per chain for within-chain parallelism.}

\item{adapt_delta}{Numeric. Target average proposal acceptance probability. Default 0.995.}

\item{max_treedepth}{Integer. Max tree depth for NUTS. Default 20.}

\item{backend}{Character. "cmdstanr" (default) or "rstan".}

\item{iso3, admin_level, admin_name}{Optional strings to locate the dataset if \code{dataset} is NULL.}

\item{write_output}{Logical. Whether to save the fitted model to disk.}

\item{output_path, input_dir}{Paths for output/input.}

\item{save_pars}{Logical. Forwarded to \code{brms::brm}.}

\item{verbose}{Logical.}
}
\value{
The fitted \code{brmsfit} object.
}
\description{
Fits a Bayesian GLM/GAM on the Mosquito Alert occupancy dataset using \code{brms}.
This model does NOT include the spatial BYM2 random effect. It uses the same
processed data structure as \code{run_brms_bym2_model()} but ignores the adjacency
matrix.
}
