% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_landcover_data.R
\name{process_landcover_data}
\alias{process_landcover_data}
\title{Process land-cover raster by clipping to a GADM boundary}
\usage{
process_landcover_data(
  landcover,
  boundary,
  write_raster = TRUE,
  output_filename = NULL,
  proc_dir = "data/proc",
  datatype = "INT1U",
  verbose = TRUE
)
}
\arguments{
\item{landcover}{A \code{terra::SpatRaster} or path to a land-cover raster file.}

\item{boundary}{An \code{sf} polygon layer (e.g., the output of
\code{\link[=get_gadm_data]{get_gadm_data()}}) defining the target administrative unit.}

\item{write_raster}{Logical. If \code{TRUE}, write the masked raster using
\code{\link[terra:writeRaster]{terra::writeRaster()}}.}

\item{output_filename}{Optional file name for the raster output. If \code{NULL}
and \code{landcover} is supplied as a file path, the base name of the raster is
reused with \code{"_processed.tif"} appended. Otherwise defaults to
\code{"landcover_processed.tif"}.}

\item{proc_dir}{Directory used when \code{write_raster = TRUE}. Defaults to
\code{"data/proc"}.}

\item{datatype}{GDAL datatype passed to \code{\link[terra:writeRaster]{terra::writeRaster()}}. Defaults to
\code{"INT1U"}, which preserves the categorical land-cover codes.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages.}
}
\value{
A list containing:
\itemize{
\item \code{mask}: the cropped/masked \code{terra::SpatRaster} with an attached attribute
table mapping class codes to names.
\item \code{legend}: a tibble with \code{class_code}, \code{class_name}, and \code{color}
associations for plotting convenience.
}
}
\description{
This helper loads a land-cover raster (e.g., ESA WorldCover GeoTIFF), crops it
to the extent of a GADM administrative boundary, masks pixels outside the
boundary, attaches an ESA WorldCover attribute table, and returns the masked
\code{terra::SpatRaster}. Optionally, the masked layer can be written to disk via
\code{\link[terra:writeRaster]{terra::writeRaster()}}.
}
