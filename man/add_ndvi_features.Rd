% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_ndvi_features.R
\name{add_ndvi_features}
\alias{add_ndvi_features}
\title{Add high-NDVI proximity metrics to Mosquito Alert model inputs}
\usage{
add_ndvi_features(
  iso3,
  admin_level,
  admin_name,
  data_dir = "data/proc",
  ndvi_threshold = 0.3,
  decay_alpha = 0.01,
  decay_beta = 1,
  verbose = TRUE
)
}
\arguments{
\item{iso3}{Three-letter ISO3 country code.}

\item{admin_level}{Administrative level used to build the dataset slug.}

\item{admin_name}{Administrative unit name used in the file naming scheme.}

\item{data_dir}{Directory containing processed datasets. Defaults to
\code{"data/proc"}.}

\item{ndvi_threshold}{Numeric cutoff applied to the NDVI raster. Defaults to
\code{0.3}.}

\item{decay_alpha}{Optional exponential decay coefficient used when
deriving proximity; set to \code{NULL} to skip. Defaults to \code{0.01}.}

\item{decay_beta}{Exponent applied to the distance term in the decay
function. Ignored when \code{decay_alpha} is \code{NULL}. Defaults to \code{1}.}

\item{verbose}{Logical; if \code{TRUE}, prints progress messages.}
}
\value{
A tibble/data frame with the augmented variables. Attributes from
the source dataset are preserved and augmented with \code{ndvi_source},
\code{ndvi_threshold}, and \code{output_path} metadata. ndvi_distance_m – meters from each report to the closest raster cell whose NDVI ≥ threshold.
ndvi_value_nearest – NDVI value at that nearest qualifying cell.
ndvi_ddf_proximity – optional exponential decay score exp(-α · distance^β); higher values mean nearer high-NDVI areas.
}
\description{
Loads the land-cover enriched model-preparation dataset produced by
\code{\link[=add_landcover_features]{add_landcover_features()}}, identifies raster cells whose NDVI exceeds a
supplied threshold, and measures each report's distance to the nearest
qualifying cell. Results are saved as \verb{model_prep_*_wx_lc_ndvi.Rds}.
}
