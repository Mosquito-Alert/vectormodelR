% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_inla_model.R
\name{run_inla_model}
\alias{run_inla_model}
\title{Fit a standard (non-spatial) Mosquito Alert occupancy model with INLA}
\usage{
run_inla_model(
  dataset = NULL,
  formula,
  cellsize_m = 800,
  temporal_resolution = c("daily", "hourly"),
  family = "binomial",
  Ntrials = 1,
  control_compute = list(dic = TRUE, waic = TRUE, cpo = TRUE, config = TRUE),
  control_predictor = list(compute = TRUE),
  control_inla = NULL,
  iso3 = NULL,
  admin_level = NULL,
  admin_name = NULL,
  write_output = TRUE,
  output_path = "data/proc",
  input_dir = "data/proc",
  verbose = TRUE
)
}
\arguments{
\item{dataset}{A prepared modelling object (e.g. a \code{brms_data_prep} created
by \code{\link[=prepare_model_data]{prepare_model_data()}}), a data frame, a path to a prepared RDS file, or
\code{NULL}. If \code{NULL}, \code{iso3}, \code{admin_level}, and \code{admin_name} are used to
locate a prepared object in \code{input_dir}.}

\item{formula}{A formula or character string giving the fixed and random
effects structure. Required.}

\item{cellsize_m}{Numeric cell size in metres, used only for prepared-file
lookup. Defaults to \code{800}.}

\item{temporal_resolution}{Character. Either \code{"daily"} or \code{"hourly"}; used
for prepared-file lookup when \code{dataset = NULL}.}

\item{family}{Character INLA likelihood. Defaults to \code{"binomial"} (the INLA
equivalent of a Bernoulli occupancy likelihood).}

\item{Ntrials}{Number of binomial trials, passed to \code{INLA::inla()} when
\code{family = "binomial"}. Defaults to \code{1}.}

\item{control_compute}{List passed to \code{INLA::inla(control.compute = ...)}.}

\item{control_predictor}{List passed to \code{INLA::inla(control.predictor = ...)}.}

\item{control_inla}{Optional list passed to \code{INLA::inla(control.inla = ...)}.}

\item{iso3, admin_level, admin_name}{Optional location identifiers used for
prepared-file lookup when \code{dataset = NULL}.}

\item{write_output}{Logical. Whether to save the fitted model to disk.}

\item{output_path}{Directory or file path for saved model output.}

\item{input_dir}{Directory used when automatically locating prepared data.}

\item{verbose}{Logical. Emit messages when \code{TRUE}.}
}
\value{
The fitted \code{inla} object, with attributes for the model data,
temporal resolution, formula text, source dataset, location slug, and
output path.
}
\description{
Fits an occupancy model on the prepared Mosquito Alert modelling data using
R-INLA. This is the INLA counterpart to \code{\link[=run_brms_model]{run_brms_model()}}: it consumes the
same prepared data produced by \code{\link[=prepare_model_data]{prepare_model_data()}} and does NOT include a
spatial BYM2 term. For the spatial version, use \code{\link[=run_inla_bym2_model]{run_inla_bym2_model()}}.
}
