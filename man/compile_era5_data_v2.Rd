% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compile_era5_data_v2.R
\name{compile_era5_data_v2}
\alias{compile_era5_data_v2}
\title{Compile ERA5 monthly CSVs from GRIB files (terra-only)}
\usage{
compile_era5_data_v2(
  input_dir = NULL,
  processed_dir = NULL,
  iso3 = NULL,
  recent_n = 3,
  verbose = TRUE
)
}
\arguments{
\item{input_dir}{Directory containing per-variable GRIB files. If NULL/empty and \code{iso3}
supplied, defaults to \code{file.path("data/weather/grib", tolower(iso3))}.}

\item{processed_dir}{Output dir for monthly CSVs. Defaults to \verb{<input_dir>/processed}.}

\item{iso3}{Optional ISO3 code (character). Used to resolve \code{input_dir} (when missing)
and to build output filenames.}

\item{recent_n}{Number of most recent monthly CSVs to merge into a summary (default 3).}

\item{verbose}{Print progress.}
}
\value{
(invisibly) a list with summary info.
}
\description{
Scans \code{input_dir} for files named \verb{era5_<iso3>_YYYY_MM_<var>.grib}, converts each
month into a single long-format CSV.GZ under \verb{processed/YYYY/}, and writes a
recent N-month summary in \code{input_dir}.
}
