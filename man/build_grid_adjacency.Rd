% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_grid_adjacency.R
\name{build_grid_adjacency}
\alias{build_grid_adjacency}
\title{Build adjacency matrix for a grid (global-safe)}
\usage{
build_grid_adjacency(
  iso3,
  admin_level,
  admin_name,
  data_dir = "data/proc",
  cellsize_m = 400,
  target_crs = NULL,
  sparse = TRUE,
  model
)
}
\arguments{
\item{iso3}{Three-letter ISO3 country code used in the slug.}

\item{admin_level}{Administrative level tied to the boundary/grid slug.}

\item{admin_name}{Administrative unit name used in the file naming scheme.}

\item{data_dir}{Directory holding processed outputs. Defaults to \code{"data/proc"}.}

\item{cellsize_m}{Numeric cell size (meters) for the hex grid file name.
Defaults to 400. Use values matching the \code{build_spatial_grid()} output
(for example, setting \code{cellsize_m = 800} targets
\verb{spatial_*_hex_grid_800.Rds} and the corresponding \code{grid_id_800} column).}

\item{target_crs}{Optional. EPSG code or \code{sf::st_crs()} object. If \code{NULL},
a suitable CRS is chosen automatically (UTM if input is lon/lat).}

\item{sparse}{Logical. If TRUE (default), return a sparse Matrix.}

\item{model}{Either the in-memory model dataset or a path to a prepared
\verb{model_prep_*} RDS file. This must be supplied; the function no longer
derives the model path automatically.}
}
\value{
An adjacency matrix (dense base matrix if \code{sparse = FALSE},
otherwise a \code{Matrix::dgCMatrix}) with row/col names equal to the
size-specific grid identifier (for example \code{grid_id_400}).
}
\description{
Reads a polygon grid (\verb{spatial_*_hex_grid_<cellsize>.Rds}) and prepared model
data (\verb{model_prep_*_wx_lc_ndvi_elev.Rds}), keeps only cells present in model
data, reprojects (only if needed) to a locally appropriate projected CRS, and
returns a binary queen-contiguity adjacency matrix suitable for
\code{brms::car()}. The function first searches for size-specific grid filenames
and falls back to legacy names without the \verb{<cellsize>} token.
}
