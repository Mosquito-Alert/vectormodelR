% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_features.R
\name{add_features}
\alias{add_features}
\title{Sequentially enrich model-preparation datasets}
\usage{
add_features(
  iso3,
  admin_level,
  admin_name,
  features,
  temporal_resolution = c("daily", "hourly"),
  vector_sources = c("malert", "gbif"),
  data_dir = "data/proc",
  verbose = TRUE
)
}
\arguments{
\item{iso3}{Three-letter ISO3 country code.}

\item{admin_level}{Administrative level used when preparing the dataset.}

\item{admin_name}{Administrative unit name.}

\item{features}{Character vector or comma-separated feature codes. Available
codes are \code{"hex"}, \code{"hex_<cellsize>"}, \code{"h3_<resolution>"}, \code{"wx_land"},
\code{"wx_single"}, \code{"lc"}, \code{"ndvi"}, \code{"el"}, \code{"pd"}, \code{"se"},
\code{"se_<factor>"}, and \code{"se_<trs_factor>_<tgb_factor>"}.}

\item{temporal_resolution}{Either \code{"daily"} or \code{"hourly"}. This controls
weather enrichment and pseudoabsence generation.}

\item{vector_sources}{Vector data sources used to prepare the base dataset.
Accepted values are \code{"malert"} and \code{"gbif"}.}

\item{data_dir}{Directory containing processed data.}

\item{verbose}{Logical. Print progress messages.}
}
\value{
The enriched dataset.
}
\description{
Locates a prepared model dataset and adds requested features in the supplied
order. Weather resolution is controlled by \code{temporal_resolution}. When \code{se}
is requested, the most recent preceding grid feature determines the shared
cell-ID column: \verb{h3_<resolution>} maps to \verb{h3_id_<resolution>}, and
\verb{hex_<cellsize>} maps to \verb{hex_id_<cellsize>}.
}
\details{
Sampling-effort codes can optionally specify pseudoabsence sampling factors:
\code{"se"} uses the defaults from \code{add_pseudoabsences_se()}, \code{"se_7"} uses 7 for
both TRS and TGB, and \code{"se_7_5"} uses 7 for TRS and 5 for TGB.
}
\examples{
\dontrun{
daily_data <- add_features(
  iso3 = "ESP",
  admin_level = 4,
  admin_name = "Barcelona",
  temporal_resolution = "daily",
  features = "h3_9,se_7,el,pd,wx_land,ndvi,lc"
)

hourly_data <- add_features(
  iso3 = "ESP",
  admin_level = 4,
  admin_name = "Barcelona",
  temporal_resolution = "hourly",
  features = "hex_1200,se_7_5,el,pd,wx_land,ndvi,lc"
)
}
}
