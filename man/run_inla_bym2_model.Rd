% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_inla_bym2_model.R
\name{run_inla_bym2_model}
\alias{run_inla_bym2_model}
\title{Fit a BYM2 Mosquito Alert occupancy model with INLA}
\usage{
run_inla_bym2_model(
  dataset = NULL,
  formula,
  cellsize_m = 800,
  temporal_resolution = c("daily", "hourly"),
  family = "binomial",
  Ntrials = 1,
  bym2_hyper = NULL,
  scale_model = TRUE,
  constr = TRUE,
  control_compute = list(dic = TRUE, waic = TRUE, cpo = TRUE, config = TRUE),
  control_predictor = list(compute = TRUE),
  control_inla = NULL,
  iso3 = NULL,
  admin_level = NULL,
  admin_name = NULL,
  write_output = TRUE,
  output_path = "data/proc",
  input_dir = "data/proc",
  verbose = TRUE
)
}
\arguments{
\item{dataset}{An \code{inla_bym2_data_prep} object, a prepared modelling object,
a data frame, a path to a prepared RDS file, or \code{NULL}. If \code{NULL}, \code{iso3},
\code{admin_level}, and \code{admin_name} are used to locate a prepared INLA BYM2
object in \code{input_dir}.}

\item{formula}{A formula or character string giving the fixed and
non-spatial random effects structure. Required. The BYM2 spatial term is
appended automatically unless the formula already contains one.}

\item{cellsize_m}{Numeric cell size in metres, used for prepared-file
lookup. Defaults to \code{800}.}

\item{temporal_resolution}{Character. Either \code{"daily"} or \code{"hourly"}; used
for prepared-file lookup when \code{dataset = NULL}.}

\item{family}{Character INLA likelihood. Defaults to \code{"binomial"}.}

\item{Ntrials}{Number of binomial trials, passed to \code{INLA::inla()} when
\code{family = "binomial"}. Defaults to \code{1}.}

\item{bym2_hyper}{Optional list of BYM2 hyperparameter priors passed to the
\code{hyper} argument of the spatial \code{f()} term. If \code{NULL}, INLA defaults are
used.}

\item{scale_model}{Logical. Value of \code{scale.model} in the BYM2 term.
Defaults to \code{TRUE}.}

\item{constr}{Logical. Value of \code{constr} in the BYM2 term. Defaults to
\code{TRUE}.}

\item{control_compute}{List passed to \code{INLA::inla(control.compute = ...)}.}

\item{control_predictor}{List passed to \code{INLA::inla(control.predictor = ...)}.}

\item{control_inla}{Optional list passed to \code{INLA::inla(control.inla = ...)}.}

\item{iso3, admin_level, admin_name}{Optional location identifiers used for
prepared-file lookup when \code{dataset = NULL}.}

\item{write_output}{Logical. Whether to save the fitted model to disk.}

\item{output_path}{Directory or file path for saved model output.}

\item{input_dir}{Directory used when automatically locating prepared data.}

\item{verbose}{Logical. Emit messages when \code{TRUE}.}
}
\value{
The fitted \code{inla} object, with attributes for the model data, INLA
graph, grid column, grid index column, temporal resolution, formula text,
source dataset, location slug, and output path.
}
\description{
Fits an occupancy model using R-INLA with a spatial BYM2 random effect based
on a polygon-contiguity graph. This is the INLA counterpart to
\code{\link[=run_brms_bym2_model]{run_brms_bym2_model()}}. It expects an \code{inla_bym2_data_prep} object created
by \code{\link[=prepare_inla_bym2_data]{prepare_inla_bym2_data()}}, a path to such an object, or a prepared
modelling object / data frame, which is passed through
\code{\link[=prepare_inla_bym2_data]{prepare_inla_bym2_data()}} internally (the polygon grid is located
automatically from \code{input_dir}).
}
\details{
If the supplied \code{formula} does not already contain a BYM2 \code{f(...)} term, the
spatial term
\verb{+ f(grid_index, model = "bym2", graph = <graph>, scale.model = TRUE, constr = TRUE)}
is appended automatically (mirroring how \code{\link[=run_brms_bym2_model]{run_brms_bym2_model()}} appends a
\code{car()} term).
}
