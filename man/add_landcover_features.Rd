% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_landcover_features.R
\name{add_landcover_features}
\alias{add_landcover_features}
\title{Add land-cover codes and classes to Mosquito Alert model inputs}
\usage{
add_landcover_features(
  dataset,
  data_dir = "data/proc",
  verbose = TRUE,
  write_output = TRUE
)
}
\arguments{
\item{dataset}{Either the in-memory weather-enriched dataset (output of
\code{\link[=add_weather_features]{add_weather_features()}}) or a path to the corresponding RDS file. When a
data object is provided it must carry an \code{output_path} attribute naming the
most recently saved file; the land-cover enriched output is written to
\code{data_dir} with \verb{_lc.Rds} appended to that stem when \code{write_output} is
\code{TRUE}.}

\item{data_dir}{Directory holding the processed rasters and where the output
dataset will be written. Defaults to \code{"data/proc"}.}

\item{verbose}{Logical; if \code{TRUE}, prints status messages while processing.}

\item{write_output}{Logical flag; when \code{TRUE} (default) the enriched dataset
is written to disk. Set to \code{FALSE} to skip writing while still returning the
augmented object and updating its metadata.}
}
\value{
A tibble/data frame containing the augmented dataset. Attributes
from the input object are preserved, and two additional attributes are set:
\code{landcover_source} (the raster path) and \code{output_path} (the saved file).
}
\description{
Loads the weather-enriched model-preparation dataset generated by
\code{\link[=add_weather_features]{add_weather_features()}}, extracts land-cover classes from the corresponding
raster, and writes a new \verb{model_prep_*_wx_lc.Rds} file that appends both the
numeric code and descriptive class.
}
