% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret_brms_model.R
\name{interpret_brms_model}
\alias{interpret_brms_model}
\title{Generate a diagnostic report for a fitted brms model}
\usage{
interpret_brms_model(
  model,
  out_file = NULL,
  format = c("html_document", "pdf_document", "word_document"),
  title = NULL,
  metadata = NULL
)
}
\arguments{
\item{model}{Either a path to an RDS file containing a \code{brmsfit} object or an
in-memory \code{brmsfit} object.}

\item{out_file}{Optional output file path or directory. If a directory (or
\code{NULL}), a filename of the form \verb{model_<slug>_brms_report.<ext>} is created
inside it, where \verb{<slug>} is taken from the model metadata when available.}

\item{format}{Document format passed to \code{rmarkdown::render()}. Defaults to
HTML but PDF and Word are supported (\code{"html_document"},
\code{"pdf_document"}, \code{"word_document"}).}

\item{title}{Optional report title. If omitted, a sensible default based on
the location slug or file name is used.}

\item{metadata}{Optional named list of additional metadata fields to include
in the report's metadata table. These entries augment (and override where
keys collide) the metadata discovered on the model object.}
}
\value{
Absolute path to the rendered report.
}
\description{
Builds a lightweight R Markdown summary for a Mosquito Alert modelling model
fitted with \code{brms}, including convergence diagnostics, parameter tables, and
a posterior predictive check. The function accepts either a previously saved
model file (RDS) or an in-memory \code{brmsfit} object and can leverage metadata
stored on the fitted object (for example the \code{location_slug} attribute set by
\code{run_brms_model()}) to drive naming and reporting.
}
