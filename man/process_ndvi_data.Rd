% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_ndvi_data.R
\name{process_ndvi_data}
\alias{process_ndvi_data}
\title{Process NDVI raster by clipping to an administrative boundary}
\usage{
process_ndvi_data(
  ndvi,
  iso3,
  admin_level,
  admin_name,
  write_raster = TRUE,
  write_stats = TRUE,
  proc_dir = "data/proc",
  datatype = "FLT4S",
  verbose = TRUE
)
}
\arguments{
\item{ndvi}{A \code{terra::SpatRaster} or path to an NDVI GeoTIFF file.}

\item{iso3}{Three-letter ISO3 country code used to locate the boundary file.}

\item{admin_level}{Administrative level associated with the boundary.}

\item{admin_name}{Administrative unit name used in the file naming scheme.}

\item{write_raster}{Logical. If \code{TRUE}, save the masked raster via
\code{\link[terra:writeRaster]{terra::writeRaster()}}.}

\item{write_stats}{Logical. If \code{TRUE}, persist the summary statistics to
disk (RDS).}

\item{proc_dir}{Directory used for any outputs. Created when missing.}

\item{datatype}{GDAL datatype passed to \code{\link[terra:writeRaster]{terra::writeRaster()}}. Defaults to
\code{"FLT4S"} for floating-point NDVI values.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages.}
}
\value{
A list with the masked NDVI raster (\code{raster}) and a tibble of
summary statistics (\code{stats}).
}
\description{
Loads an NDVI raster (e.g., a Sentinel-2 composite), aligns it with a target
boundary, crops and masks pixels outside the polygon, writes the masked
output if requested, and computes simple global summary statistics.
}
