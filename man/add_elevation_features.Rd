% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_elevation_features.R
\name{add_elevation_features}
\alias{add_elevation_features}
\title{Add elevation to Mosquito Alert model inputs}
\usage{
add_elevation_features(
  iso3,
  admin_level,
  admin_name,
  data_dir = "data/proc",
  verbose = TRUE
)
}
\arguments{
\item{iso3}{Three-letter ISO3 country code.}

\item{admin_level}{Administrative level used when sourcing the inputs.}

\item{admin_name}{Administrative unit name used in the file naming scheme.}

\item{data_dir}{Directory holding the processed datasets. Defaults to
\code{"data/proc"}.}

\item{verbose}{Logical; if \code{TRUE}, prints status messages while processing.}
}
\value{
A tibble/data frame containing the augmented dataset. Attributes
from the input object are preserved, and two additional attributes are set:
\code{elevation_source} (the raster path) and \code{output_path} (the saved file).
}
\description{
Loads the NDVI-enriched model-preparation dataset produced by
\code{\link[=add_ndvi_features]{add_ndvi_features()}}, extracts elevation values from a corresponding raster,
and writes a new \verb{model_prep_*_wx_lc_ndvi_elev.Rds} file with an
\code{elevation_m} column.
}
\details{
The elevation raster is expected at
\code{file.path(data_dir, paste0("spatial_", slug, "_elevation.tif"))}.
}
