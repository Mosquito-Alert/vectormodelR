% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_elevation_features.R
\name{add_elevation_features}
\alias{add_elevation_features}
\title{Add elevation to Mosquito Alert model inputs}
\usage{
add_elevation_features(
  dataset,
  data_dir = "data/proc",
  verbose = TRUE,
  write_output = TRUE
)
}
\arguments{
\item{dataset}{Either the in-memory model-preparation dataset or a path to
the corresponding RDS file. When a data object is provided it must carry an
\code{output_path} attribute naming the most recently saved file; the
elevation-enriched dataset is written to \code{data_dir} with \verb{_el.Rds}
appended to that stem when \code{write_output} is \code{TRUE}.}

\item{data_dir}{Directory that houses the processed rasters and where the
elevation-enriched dataset will be written. Defaults to \code{"data/proc"}.}

\item{verbose}{Logical; if \code{TRUE}, prints status messages while processing.}

\item{write_output}{Logical flag; when \code{TRUE} (default) the enriched dataset
is written to disk. Set to \code{FALSE} to skip writing while still returning the
augmented object and updating its metadata.}
}
\value{
A tibble/data frame containing the augmented dataset. Attributes
from the input object are preserved, and two additional attributes are set:
\code{elevation_source} (the raster path) and \code{output_path} (the saved file).
}
\description{
Takes a model-preparation dataset, extracts elevation values from a
corresponding raster, and writes a new RDS file with an \code{elevation_m} column
and \verb{_el} appended to the original filename when output is enabled.
}
\details{
The elevation raster is expected at
\code{file.path(data_dir, paste0("spatial_", slug, "_elevation.tif"))}.
}
