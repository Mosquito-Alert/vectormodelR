% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_h3_grid.R
\name{build_h3_grid}
\alias{build_h3_grid}
\title{Build an H3 grid for a supplied polygon}
\usage{
build_h3_grid(
  map = NULL,
  resolution = 9L,
  clip = FALSE,
  return_crs = 4326,
  write = TRUE,
  iso3 = NULL,
  admin_level = NULL,
  admin_name = NULL,
  data_dir = "data/proc",
  verbose = TRUE
)
}
\arguments{
\item{map}{Optional \code{sf} object describing the polygon(s) to cover.}

\item{resolution}{Integer H3 resolution from 0 to 15. Defaults to 9.}

\item{clip}{Logical. If \code{TRUE}, clips cells to the polygon boundary.
Defaults to \code{FALSE}.}

\item{return_crs}{Coordinate reference system for the returned grid.
Defaults to EPSG:4326.}

\item{write}{Logical. If \code{TRUE}, writes the grid to \code{data_dir}.}

\item{iso3}{Optional ISO3 country code used with \code{admin_level} and
\code{admin_name} when \code{map} is not supplied.}

\item{admin_level}{Optional administrative level.}

\item{admin_name}{Optional administrative unit name.}

\item{data_dir}{Directory containing boundary inputs and grid outputs.}

\item{verbose}{Logical. If \code{TRUE}, reports progress and file paths.}
}
\value{
An \code{sf} object containing \code{h3_id}, \code{grid_id},
\verb{h3_id_<resolution>}, \code{h3_resolution}, and polygon geometry.
}
\description{
Creates an H3 grid at a specified resolution and returns the cells as an
\code{sf} object. Cells are selected when their centres fall inside the supplied
polygon.
}
\details{
H3 uses integer resolutions from 0 to 15 rather than exact cell sizes in
metres. By default, complete H3 geometries are retained so their identifiers,
boundaries, and neighbour relationships remain valid.
}
\examples{
\dontrun{
h3_grid <- build_h3_grid(
  iso3 = "ESP",
  admin_level = 4,
  admin_name = "Barcelona",
  resolution = 9,
  write = TRUE
)

h3_grid <- build_h3_grid(
  map = bcn_poly,
  resolution = 9,
  clip = FALSE,
  write = FALSE
)
}

}
