% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_inla_bym2.R
\name{prepare_inla_bym2_data}
\alias{prepare_inla_bym2_data}
\title{Prepare spatial data for an INLA BYM2 model}
\usage{
prepare_inla_bym2_data(
  dataset,
  queen = TRUE,
  snap = NULL,
  make_valid = TRUE,
  drop_unused_grid = TRUE,
  allow_islands = FALSE,
  allow_disconnected = TRUE,
  data_dir = "data/proc",
  output_dir = "data/proc",
  graph_filename = NULL,
  write = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{dataset}{A \code{model_data_prep} object, a legacy \code{brms_data_prep}
object, a data frame, or a path to an RDS file containing one of these
objects.}

\item{queen}{Logical. If \code{TRUE}, polygons sharing an edge or corner are
neighbours. If \code{FALSE}, only polygons sharing an edge are neighbours.}

\item{snap}{Optional numeric snapping tolerance passed to
\code{spdep::poly2nb()}. If \code{NULL}, the package default is used.}

\item{make_valid}{Logical. If \code{TRUE}, invalid polygon geometries are repaired
using \code{\link[sf:valid]{sf::st_make_valid()}}.}

\item{drop_unused_grid}{Logical. If \code{TRUE}, polygons without model
observations are removed before constructing the graph.}

\item{allow_islands}{Logical. Whether polygons with no neighbours are allowed.
Defaults to \code{FALSE}.}

\item{allow_disconnected}{Logical. Whether more than one connected spatial
component is allowed. Defaults to \code{TRUE}; a warning is issued when the graph
contains multiple components.}

\item{data_dir}{Directory searched for the polygon grid file, located
automatically from the prepared object's location slug and cell size.
Defaults to \code{"data/proc"}.}

\item{output_dir}{Directory used when \code{write = TRUE}.}

\item{graph_filename}{Optional graph filename. If \code{NULL}, a filename is
generated from the location slug and grid column.}

\item{write}{Logical. If \code{TRUE}, save the prepared object and retain the graph
file in \code{output_dir}.}

\item{verbose}{Logical. Emit informative messages when \code{TRUE}.}
}
\value{
An object of class \code{inla_bym2_data_prep} containing:
\item{model_data}{Prepared modelling data with \code{grid_index} and
\code{component_id} columns.}
\item{grid}{Ordered \code{sf} polygons with matching \code{grid_index} and
\code{component_id} values.}
\item{grid_col}{The grid identifier column.}
\item{grid_index_col}{The name \code{"grid_index"}.}
\item{node_map}{Mapping between original grid identifiers, INLA node
indices, and connected components.}
\item{neighbours}{An \code{nb} neighbourhood object.}
\item{graph}{An INLA graph object.}
\item{graph_path}{Persistent path to the adjacency graph when
\code{write = TRUE}; otherwise \code{NULL}.}
\item{meta}{Spatial preparation metadata.}
}
\description{
Aligns prepared modelling data with a polygon grid, creates a stable integer
spatial index, constructs polygon-contiguity neighbours, writes an INLA graph,
and returns an object ready for fitting a BYM2 spatial model with R-INLA.
}
\details{
This function is intended to be called after \code{\link[=prepare_model_data]{prepare_model_data()}}.
It does not repeat filtering, temporal aggregation, factor conversion, or
predictor scaling.
}
